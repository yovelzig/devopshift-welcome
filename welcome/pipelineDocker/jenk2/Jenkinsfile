pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile'
            dir 'jenk2'
        }
    }
    stages {
        stage('Build') {
            steps {
                echo "npm install"
            }
        }
    }
}