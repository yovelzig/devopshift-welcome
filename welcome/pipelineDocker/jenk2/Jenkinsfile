pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile'
            dir 'docker'
        }
    }
    stages {
        stage('Build') {
            steps {
                echo "npm install"
            }
        }
    }
}